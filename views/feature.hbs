{{> layout_start_document}}
{{> layout_start_body}}
<main>
  <header>
    <h1>Your Feature</h1>
  </header>
  <section>
    {{#each features.features}}
      {{this}}<br>
    {{else}}
      <p>Sorry, no feature found</p>
    {{/each}}
  </section>
</main>
{{> layout_end}}
