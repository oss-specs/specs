<li>
  <a class="spec-link" href="{{this.route}}?ref={{currentBranchName}}">
    <section class="file-container">
      <section class="file-details">
        <p class="file-name">
          {{#if this.empty}}
            <span class="empty-file" title="This file is empty" >&#x2205;&nbsp;&nbsp;{{this.name}}</span>
          {{else}}
            {{#if this.error}}
              <span class="parse-error" title="Error parsing file.">&#x1F645;&nbsp;&nbsp;{{this.name}}</span>
            {{else}}
              <span title="{{this.filePath}}">{{this.name}}</span>
            {{/if}}
          {{/if}}
        </p>
      </section>
    </section>
  </a>
</li>
